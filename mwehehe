<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happy Birthday, Chay!</title>
  <style>
    body {
      margin: 0;
      background: pink;
      overflow: hidden;
      font-family: "Poppins", sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* INTRO SCREEN */
    .intro {
      position: absolute;
      inset: 0;
      background: pink;
      display: flex;
      justify-content: center;
      align-items: center;
      transition: opacity 1s ease;
      z-index: 10;
    }

    .intro button {
      font-size: 40px;
      color: white;
      background: #ff69b4;
      border: none;
      border-radius: 50px;
      padding: 20px 50px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.2);
      cursor: pointer;
      transition: transform 0.2s, background 0.3s;
      animation: float 2s ease-in-out infinite;
    }

    .intro button:hover {
      background: #ff1493;
      transform: scale(1.05);
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    /* MAIN SCENE (hidden at first) */
    .scene-container {
      opacity: 0;
      transition: opacity 1s ease;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      width: 100%;
      height: 100vh;
      display: none;
    }

    h1.title {
      font-size: 45px;
      color: white;
      text-shadow: 2px 2px 8px #ff69b4;
      margin-top: 70px;
      margin-bottom: 40px;
      animation: float 3s ease-in-out infinite;
    }

    .scene {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 100px;
      flex-grow: 1;
    }

    /* Cake */
    .cake {
      position: relative;
      width: 220px;
      height: 130px;
      background: linear-gradient(to bottom, #ffc0cb, #ff69b4);
      border-radius: 12px;
      box-shadow: inset 0 5px #fff, 0 10px 25px rgba(0,0,0,0.2);
    }

    .cake::before {
      content: "";
      position: absolute;
      top: 18px;
      left: 0;
      width: 100%;
      height: 18px;
      background: #fff0f5;
      border-radius: 10px;
      box-shadow: inset 0 -3px #ff69b4;
    }

    .candle {
      position: absolute;
      width: 10px;
      height: 45px;
      background: white;
      top: -45px;
      border-radius: 3px;
    }

    .candle:nth-child(1) { left: 50px; }
    .candle:nth-child(2) { left: 105px; }
    .candle:nth-child(3) { left: 160px; }

    .flame {
      position: absolute;
      top: -15px;
      left: 50%;
      transform: translateX(-50%);
      width: 10px;
      height: 15px;
      background: radial-gradient(circle, gold 40%, orange 70%, transparent 100%);
      border-radius: 50%;
      animation: flicker 0.3s infinite alternate;
    }

    @keyframes flicker {
      0% { transform: translateX(-50%) scale(1); opacity: 0.9; }
      100% { transform: translateX(-50%) scale(1.2); opacity: 0.7; }
    }

    /* Letter */
    .letter {
      position: relative;
      width: 240px;
      height: 160px;
      background: #fff;
      border: 2px solid #ff69b4;
      border-radius: 8px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      overflow: hidden;
      transform-style: preserve-3d;
      perspective: 800px;
      cursor: pointer;
    }

    .flap {
      position: absolute;
      top: 0;
      left: 0;
      width: 0;
      height: 0;
      border-left: 120px solid transparent;
      border-right: 120px solid transparent;
      border-bottom: 80px solid #ffb6c1;
      transform-origin: top center;
      transition: transform 0.8s ease;
      z-index: 2;
    }

    .letter.open .flap {
      transform: rotateX(-180deg);
    }

    .content {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background: #fff;
      font-size: 16px;
      color: #333;
      text-align: center;
      opacity: 0;
      transition: opacity 0.8s ease;
      padding: 10px;
      box-sizing: border-box;
    }

    .letter.open .content {
      opacity: 1;
    }

    .from-line {
      margin-top: 15px;
      font-size: 14px;
      color: #ff69b4;
      font-style: italic;
    }

    /* Confetti */
    .confetti {
      position: absolute;
      width: 8px;
      height: 8px;
      background: hsl(var(--hue), 70%, 60%);
      top: 0;
      left: 50%;
      opacity: 0.8;
      animation: fall linear forwards;
    }

    @keyframes fall {
      0% { transform: translateY(0) rotate(0deg); }
      100% { transform: translateY(100vh) rotate(360deg); }
    }
  </style>
</head>
<body>
  <!-- INTRO -->
  <div class="intro" id="intro">
    <button id="hiBtn">hellu bunny, click this pweasee!!😗 </button>
  </div>

  <!-- MAIN SCENE -->
  <div class="scene-container" id="sceneContainer">
    <h1 class="title">🎉 happyy birthhdayy!! 🎉</h1>
    <div class="scene">
      <div class="cake">
        <div class="candle"><div class="flame"></div></div>
        <div class="candle"><div class="flame"></div></div>
        <div class="candle"><div class="flame"></div></div>
      </div>

      <div class="letter" id="letter">
        <div class="flap"></div>
        <div class="content">
          <p>Happy birthday, Chay. I miss you!! 🩷</p>
          <div class="from-line">From: 🦖 </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    const intro = document.getElementById("intro");
    const sceneContainer = document.getElementById("sceneContainer");
    const hiBtn = document.getElementById("hiBtn");
    const letter = document.getElementById("letter");

    // click "Hi" to show surprise
    hiBtn.addEventListener("click", () => {
      intro.style.opacity = "0";
      setTimeout(() => {
        intro.style.display = "none";
        sceneContainer.style.display = "flex";
        setTimeout(() => {
          sceneContainer.style.opacity = "1";
        }, 50);
      }, 1000);
    });

    // open letter
    letter.addEventListener("click", () => {
      letter.classList.toggle("open");
    });

    // confetti (lots of them!)
    const colors = [0, 40, 80, 120, 160, 200, 260, 300];
    function createConfetti() {
      for (let i = 0; i < 8; i++) { // more confetti per batch
        const confetti = document.createElement("div");
        confetti.classList.add("confetti");
        confetti.style.setProperty("--hue", colors[Math.floor(Math.random() * colors.length)]);
        confetti.style.left = Math.random() * 100 + "vw";
        confetti.style.animationDuration = (7 + Math.random() * 5) + "s";
        confetti.style.animationDelay = Math.random() * 2 + "s";
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 12000);
      }
    }
    setInterval(createConfetti, 300);
  </script>
</body>
</html>
